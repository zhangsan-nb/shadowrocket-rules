# Security Policy

## 安全边界

main 中的构建器、配置、安全策略、测试和工作流是可信代码。上游响应永远是不可信输入。release 是唯一生产输出分支。

以下情况均失败关闭：

- 非 HTTPS、非 200、超时、超限或不安全重定向；
- HTML、404、错误页、控制字符或未知规则类型；
- MITM、Script、URL Rewrite、Host、General、Proxy 或远程脚本指令；
- 公共后缀、顶级域、过宽 CIDR 或危险关键词；
- 跨类别语义交集，或 protected 命名空间无法证明等价；
- 空列表、低于固定下限、变化量超过阈值；
- 候选制品的路径、文件集合、大小、SHA 或身份绑定不一致；
- release 基线已变化，或远端推送结果无法精确确认。

## 报告问题

不要在公开 Issue 中粘贴凭据、Token、私有域名或未公开漏洞细节。请使用 GitHub Security Advisory 的私密报告功能。仓库未上线前，请直接联系仓库所有者。

报告应包含受影响 commit、最小复现、预期与实际门禁行为，以及是否可能改变 release。

## 响应原则

1. 暂停自动发布，保留 last-known-good release。
2. 保存可疑输入的 SHA-256 与 provenance，不重新解释或发布。
3. 修复必须新增回归测试，不能通过放宽门禁解决。
4. 通过全部验证后，用普通后继 commit 发布。
5. 回滚也使用普通 commit，不 force push、不重写历史。

## V1 范围外

V1 不进行 WHOIS、实时 DNS 或全球探测、证书信誉、ASN 风险评分、机器学习或上游多数投票。这些信号不能替代本仓库的确定性门禁。
